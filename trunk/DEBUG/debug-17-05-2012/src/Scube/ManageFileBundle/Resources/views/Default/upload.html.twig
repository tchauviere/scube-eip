<!DOCTYPE html>
<html>
    <head>
        <title>Scube</title>
    </head>
    <body>
		<form action="{{ path('ScubeManageFileBundle_upload') }}" method="post" {{ form_enctype(form) }}>
			{{ form_widget(form) }}
			{{ form_widget(form._token) }}
			<input type="submit" />
		</form>
    </body>
</html>